#include <iostream>

int main()
{
    std::string cmds; 
    std::cout << "enter commends {ADD, SEARCH, EXIT}." << std::endl;
    std::getline(std::cin, cmds);
    while (1)
    {
        if (cmds == "ADD")
        {

        }
        if (cmds == "SEARCH")
        {

        }
        if (cmds == "EXIT")
        {

        }
    }
    return (0);
}